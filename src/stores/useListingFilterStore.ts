// Listing filter store — placeholder
// TODO: implement with Zustand

// import { create } from "zustand";
//
// interface ListingFilterState {
//   search: string;
//   priceMin: number | null;
//   priceMax: number | null;
//   setSearch: (v: string) => void;
//   setPriceRange: (min: number | null, max: number | null) => void;
//   reset: () => void;
// }
//
// export const useListingFilterStore = create<ListingFilterState>((set) => ({
//   search: "",
//   priceMin: null,
//   priceMax: null,
//   setSearch: (search) => set({ search }),
//   setPriceRange: (priceMin, priceMax) => set({ priceMin, priceMax }),
//   reset: () => set({ search: "", priceMin: null, priceMax: null }),
// }));

export {};
