import type { Metadata } from "next";

export const metadata: Metadata = { title: "Profile — Ulrich Propiedades" };

export default function ProfilePage() {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <p className="text-gray-400 text-lg">Profile — coming soon</p>
    </div>
  );
}
