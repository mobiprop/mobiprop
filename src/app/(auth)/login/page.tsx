import type { Metadata } from "next";

export const metadata: Metadata = { title: "Login — Ulrich Propiedades" };

export default function LoginPage() {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <p className="text-gray-400 text-lg">Login — coming soon</p>
    </div>
  );
}
