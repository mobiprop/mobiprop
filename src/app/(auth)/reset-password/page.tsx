import type { Metadata } from "next";

export const metadata: Metadata = { title: "Reset Password — Ulrich Propiedades" };

export default function ResetPasswordPage() {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <p className="text-gray-400 text-lg">Reset Password — coming soon</p>
    </div>
  );
}
